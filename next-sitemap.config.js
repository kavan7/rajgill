// next-sitemap.config.js
module.exports = {
  siteUrl: 'https://rajgilllaw.com', // Replace with your website's URL
  generateRobotsTxt: true, // (Optional) Generates a `robots.txt` file with the sitemap URL
  sitemapSize: 7000, // (Optional) Split large sitemaps into smaller files (recommended limit is 50,000 URLs per sitemap)
}
